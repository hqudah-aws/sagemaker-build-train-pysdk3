DOCKERFILE_PATH=docker/stable_diffusion/Dockerfile.stablediffusion
ECR_REPOSITORY_NAME=stablediffusion
IMAGE_TAG=latest

# AWS_REGION and AWS_ACCOUNT_ID are auto-detected from your AWS CLI config /
# environment by push.sh, so this repo runs in any region without edits.
# Uncomment to pin explicit values instead:
# AWS_REGION=us-east-2
# AWS_ACCOUNT_ID=123456789012
